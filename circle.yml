machine:
  node:
    version: 8
  environment:
    NODE_ENV: development

dependencies:
  override:
    - yarn
  post:
    - yarn lerna bootstrap

test:
  override:
    - yarn lint
    - yarn lerna run start lint
    - yarn lerna run start test
    - yarn lerna run start e2e -- --concurrency 1
